{
  "metadata": {
    "id": "QUERY.SUBQUERY_03_DERIVED",
    "title": "Derived table",
    "module": "query",
    "feature": "query",
    "level": "P1",
    "status": "active",
    "timeout": "30s",
    "isolation": "session",
    "destructive": false,
    "tags": [
      "subquery"
    ]
  },
  "steps": [
    {
      "id": "q1",
      "kind": "query",
      "sql": "SELECT q.value FROM (SELECT 3 AS value) q",
      "comparison": {
        "mode": "exact"
      },
      "expected": {
        "rows": [
          [
            3
          ]
        ]
      }
    }
  ]
}

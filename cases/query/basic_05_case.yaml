{
  "metadata": {
    "id": "QUERY.BASIC_05_CASE",
    "title": "CASE expression",
    "module": "query",
    "feature": "query",
    "level": "P1",
    "status": "active",
    "timeout": "30s",
    "isolation": "session",
    "destructive": false,
    "tags": [
      "basic"
    ]
  },
  "steps": [
    {
      "id": "q1",
      "kind": "query",
      "sql": "SELECT CASE WHEN 1 = 1 THEN 1 ELSE 0 END AS value",
      "comparison": {
        "mode": "exact"
      },
      "expected": {
        "rows": [
          [
            1
          ]
        ]
      }
    }
  ]
}

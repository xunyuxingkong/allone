{
  "metadata": {
    "id": "QUERY.STRING_04_CONCAT",
    "title": "CONCAT function",
    "module": "query",
    "feature": "query",
    "level": "P1",
    "status": "active",
    "timeout": "30s",
    "isolation": "session",
    "destructive": false,
    "tags": [
      "string"
    ]
  },
  "steps": [
    {
      "id": "q1",
      "kind": "query",
      "sql": "SELECT CONCAT('xu', 'gu') AS value",
      "comparison": {
        "mode": "exact"
      },
      "expected": {
        "rows": [
          [
            "xugu"
          ]
        ]
      }
    }
  ]
}

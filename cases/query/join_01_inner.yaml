{
  "metadata": {
    "id": "QUERY.JOIN_01_INNER",
    "title": "INNER JOIN derived rows",
    "module": "query",
    "feature": "query",
    "level": "P1",
    "status": "active",
    "timeout": "30s",
    "isolation": "session",
    "destructive": false,
    "tags": [
      "join"
    ]
  },
  "steps": [
    {
      "id": "q1",
      "kind": "query",
      "sql": "SELECT a.n, b.label FROM (SELECT 1 AS n) a JOIN (SELECT 1 AS n, 'xugu' AS label) b ON a.n = b.n",
      "comparison": {
        "mode": "exact"
      },
      "expected": {
        "rows": [
          [
            1,
            "xugu"
          ]
        ]
      }
    }
  ]
}
